{
  # Enable which-key - a popup that shows keybindings for the command you are currently typing
  # See source: https://github.com/folke/which-key.nvim, options: https://nix-community.github.io/nixvim/search/?query=plugins.which-key&option_scope=0&option=plugins.which-key.enable
  plugins = {
    which-key = {
      enable = true;
    };
  };
}
