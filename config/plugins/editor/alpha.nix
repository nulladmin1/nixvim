{
  # Enable alpha startup screen
  # See source: https://github.com/goolord/alpha-nvim, options: https://nix-community.github.io/nixvim/search/?query=plugins.alpha&option_scope=0&option=plugins.alpha.enable
  plugins.alpha = {
    enable = true;

    # Set the theme to dashboard
    theme = "dashboard";
  };
}
